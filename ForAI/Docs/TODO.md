# TODO 功能清单

本文件只记录“当前还没实现、但后续可能要做”的功能缺口。

维护规则：

- 功能实际完成并验证后，必须从本文档移除对应条目。
- 已明确不做的能力不要放入本文档。
- 新增 TODO 时写清楚所属模块、当前状态、需要补齐的后端/前端/文档工作。
- 不要把临时 bug、个人备忘或一次性排查事项放进本文档。

## 关注系统

- [ ] 关注版块
  - 当前状态：仅有关注作者接口和页面能力。
  - 需要补齐：后端关注版块数据表/API、前端关注/取消关注入口、`/me/following` 关注版块列表、通知或动态规则。

- [ ] 关注领域
  - 当前状态：仅有关注作者接口和页面能力。
  - 需要补齐：后端关注领域数据表/API、前端关注/取消关注入口、`/me/following` 关注领域列表、通知或动态规则。

- [ ] 推荐其他用户
  - 当前状态：`/me/following` 和作者公开主页仅展示空状态，不造假推荐数据。
  - 需要补齐：推荐用户接口或明确推荐规则，前端接入推荐作者列表。

## 文章与编辑

- [ ] 投稿页实时预览
  - 当前状态：文章编辑器已有预览入口，但是否做完整实时预览仍未定稿。
  - 需要补齐：明确预览交互、Markdown 渲染边界、图片/代码块展示规则。

- [ ] 文章 slug
  - 当前状态：文章详情使用数字 ID。
  - 需要补齐：确认是否需要文章 slug；如需要，补数据库字段、唯一性规则、路由和旧链接兼容策略。

- [ ] 评论锚点跳转
  - 当前状态：通知可跳转文章，但评论锚点方案尚未实现。
  - 需要补齐：评论 DOM 锚点、通知链接 hash、进入页面后定位与高亮。

## 搜索与发现

- [ ] PostgreSQL 全文搜索评估
  - 当前状态：发现页与文章列表使用现有接口能力，未接复杂全文检索。
  - 需要补齐：确认是否使用 PostgreSQL full text search；如采用，补索引、查询排序、摘要高亮策略。

- [ ] 搜索结果高亮摘要
  - 当前状态：未实现搜索命中高亮。
  - 需要补齐：后端返回命中片段或前端安全高亮规则。

## 管理与安全

- [ ] 登录页扩展登录能力
  - 当前状态：前端登录页已按设计稿展示“记住我、忘记密码、第三方登录、用户名登录”入口，`/forgot-password` 已有前端页面壳；实际登录仍只接现有 `/auth/login` 邮箱+密码接口，找回密码不会发送邮件。
  - 需要补齐：确认用户名登录规则、记住登录态/续期策略、找回密码令牌/邮件发送/重设密码 API、GitHub/Google/微信 OAuth 后端接口、前端接入与错误提示。

- [ ] 用户角色管理 UI
  - 当前状态：角色能力已在后端存在一部分，但 MVP 暂不提供普通管理 UI。
  - 需要补齐：角色变更权限边界、管理后台页面、审计记录。

- [ ] 操作审计日志
  - 当前状态：管理员/审核员操作没有统一审计日志。
  - 需要补齐：审计表、写入点、查询页面、保留策略。

- [ ] 内容敏感词或审核队列
  - 当前状态：评论和回复默认不需要审核。
  - 需要补齐：是否引入敏感词、评论审核队列、命中后的用户提示与管理入口。

- [ ] Refresh token 与 token 轮换
  - 当前状态：使用 JWT + httpOnly Cookie，未实现 refresh token 轮换机制。
  - 需要补齐：登录态续期策略、失效策略、服务端存储或无状态方案评估。

- [ ] 密码策略细化
  - 当前状态：已要求最小长度，但具体复杂度策略未定稿。
  - 需要补齐：最小长度、字符组合、常见弱密码限制、错误提示。

## 个人资料与设置

- [ ] 扩展个人资料字段
  - 当前状态：`/me/profile` 只持久化头像、Bio、学校和公司；用户名、邮箱、GitHub、地区、技术标签、展示设置、个人封面和社交链接在设置页中仅作只读、静态展示或空状态。
  - 需要补齐：后端用户资料字段或独立 profile 表、PATCH API、公开作者主页展示规则、社交链接展示规则、前端表单保存逻辑和迁移文档。

- [ ] 用户简历
  - 当前状态：作者公开主页已预留“简历”tab，但没有数据模型、编辑入口和公开展示内容。
  - 需要补齐：简历字段/表结构、编辑页入口、公开作者主页展示规则、权限与隐私边界。

## 上传与存储

- [ ] 未绑定文章图片清理
  - 当前状态：上传后未绑定文章的图片缺少后台清理策略。
  - 需要补齐：清理周期、扫描范围、误删保护、运行方式。

- [ ] 图片物理文件清理
  - 当前状态：图片软删除不一定立即删除物理文件。
  - 需要补齐：物理删除策略、延迟删除机制、失败重试。

- [ ] 头像裁剪
  - 当前状态：头像上传可用，裁剪能力未实现。
  - 需要补齐：裁剪 UI、服务端尺寸处理或前端裁剪输出规则。

- [ ] 云对象存储迁移
  - 当前状态：第一版使用本地 `uploads`。
  - 需要补齐：R2/S3 选型、上传签名、访问 URL、迁移策略。

## 运维与生产增强

- [ ] PWA
  - 当前状态：明确为后续迭代，当前 MVP 不实现。
  - 需要补齐：manifest、service worker、离线策略、更新提示。

- [ ] 生产部署拓扑
  - 当前状态：PostgreSQL、反向代理、备份保留周期等仍是待确认项。
  - 需要补齐：服务器拓扑、Nginx/Caddy 方案、数据库备份周期、恢复演练。

- [ ] 外部错误追踪
  - 当前状态：未接入外部错误追踪服务。
  - 需要补齐：服务选型、前后端 SDK、脱敏规则、告警策略。

## 工程治理

- [ ] Handler 公共解析逻辑继续收敛
  - 当前状态：已有部分公共 request helper，domains/modules/uploads 等模块仍可继续收敛。
  - 需要补齐：统一分页、路径 ID、JSON body 解析和错误响应映射。

- [ ] 路由依赖 wiring 拆分
  - 当前状态：`routes.New` 负责组装较多 repository/service/handler。
  - 需要补齐：拆出 wiring helper，降低路由定义和依赖构造耦合。

- [ ] 事务 runner 抽取
  - 当前状态：事务辅助在 repository/service 内部分散处理。
  - 需要补齐：统一事务 runner，并补足事务失败回滚测试。
