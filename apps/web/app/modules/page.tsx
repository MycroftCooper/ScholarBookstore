import { DomainExplorer } from "@/components/domain/DomainExplorer";
import { SiteFrame } from "@/components/layout/SiteFrame";

export default function ModulesPage() {
  return (
    <SiteFrame>
      <DomainExplorer />
    </SiteFrame>
  );
}
